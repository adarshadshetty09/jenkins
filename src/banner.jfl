pipeline {
    agent any

    stages {
        stage('Run Banner Script') {
            steps {
                script {
                    // Run the banner.sh script
                    sh 'chmod +x banner.sh'
                    sh './banner.sh'
                }
            }
        }
    }
}
